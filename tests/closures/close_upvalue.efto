// Closure keeps variable alive after enclosing scope exits.
fun makeCounter() {
  var count = 0;
  fun increment() {
    count = count + 1;
    print count;
  }
  return increment;
}

var counter = makeCounter();
counter(); // expect: 1
counter(); // expect: 2
counter(); // expect: 3
